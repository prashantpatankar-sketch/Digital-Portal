# ============================================
# Django Configuration
# ============================================

# Debug Mode
# True = Development (emails print to console)
# False = Production (sends real emails via SMTP)
DEBUG=True

# Django Secret Key (Change in production!)
SECRET_KEY=your-secret-key-here-generate-a-new-one-must-be-long-and-random

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com


# ============================================
# Database Configuration
# ============================================

# Database Engine (mysql or sqlite)
DB_ENGINE=sqlite

# MySQL Configuration (if using MySQL)
DB_NAME=gram_panchayat_db
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_HOST=localhost
DB_PORT=3306


# ============================================
# Gmail SMTP Configuration (OTP Emails)
# ============================================

# Gmail SMTP Server Settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Your Gmail Address
EMAIL_HOST_USER=your-email@gmail.com

# Gmail App Password (16 characters, NO SPACES!)
# Get from: https://myaccount.google.com/apppasswords
# Example: abcdefghijklmnop (remove spaces from: abcd efgh ijkl mnop)
EMAIL_HOST_PASSWORD=your-16-char-app-password-here

# From Email Address (appears in email "From:" field)
DEFAULT_FROM_EMAIL=Digital Gram Panchayat Portal <noreply@grampanchayat.gov.in>


# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Copy this file to .env:
#    Copy-Item .env.example .env
#
# 2. Edit .env with your actual values:
#    code .env
#
# 3. For Gmail SMTP Setup:
#    a) Enable 2FA: https://myaccount.google.com/security
#    b) Generate App Password: https://myaccount.google.com/apppasswords
#       - Select App: Mail
#       - Select Device: Other (Custom name)
#       - Copy 16-character password
#       - REMOVE SPACES! (abcd efgh ijkl mnop → abcdefghijklmnop)
#    c) Update EMAIL_HOST_USER with your Gmail address
#    d) Update EMAIL_HOST_PASSWORD with App Password (no spaces)
#
# 4. Development Mode (No Gmail needed):
#    - Set DEBUG=True
#    - OTPs will print in terminal/console
#    - No email configuration needed
#
# 5. Production Mode (Real emails via Gmail):
#    - Set DEBUG=False
#    - Configure Gmail credentials above
#    - OTPs will be sent to actual email addresses
#
# 6. Security:
#    - Never commit .env to Git
#    - Add .env to .gitignore
#    - Keep credentials secret
#    - Rotate App Password every 90 days
#
# 7. Documentation:
#    - See GMAIL_SMTP_SETUP_GUIDE.md for complete setup guide
#    - See QUICK_COMMANDS.md for testing commands
#
